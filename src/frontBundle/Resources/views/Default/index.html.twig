{% extends '::base.html.twig' %}

{% block title %}DrakMovies{% endblock %}

{% block body %}

    <div class="container">
        <header class="clearfix">
            <h1>Nexus-6<span>Be afraid, be very afraid...</span></h1>
        </header>
        <div class="main">
            <ul id="og-grid" class="og-grid">
                {% for film in films %}
                    <li><a href="" data-largesrc="{{ asset(film.webPath("md")) }}" data-title="{{ film.nom }}" data-description=""><img src="{{ asset('dist/img/images-test/thumbs/1.jpg') }}" alt="film"></a></li>
                {% endfor %}
            </ul>
        </div>
    </div>

<!-- Default snippet for navigation -->

{% endblock %}